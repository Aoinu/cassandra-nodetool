{
  "name": "cassandra-nodetool",
  "description": "Cassandra Nodetool on Node.js",
  "version": "0.0.1",
  "main": "index.js",
  "author": "aoinu",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/aoinu/cassandra-nodetool.git"
  },
  "private": false,
  "bin": {
    "jsnodetool": "lib/cli.js"
  },
  "scripts": {
    "lint": "eslint lib/**/*.js",
    "lint:fix": "eslint --fix lib/**/*.js",
    "test": "jest",
    "test:ci": "jest --ci --runInBand --reporters=default --reporters=jest-junit",
    "test:cov": "jest --coverage"
  },
  "engines": {
    "node": ">=7.10.1"
  },
  "dependencies": {
    "argparse": "^1.0.10",
    "jmx": "^0.7.0"
  },
  "devDependencies": {
    "eslint": "^5.12.1",
    "eslint-plugin-jest": "^22.1.3",
    "jest": "^23.6.0",
    "jest-junit": "^6.1.0"
  }
}
